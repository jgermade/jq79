# React (keyed) — js-framework-benchmark implementation

Function component, `useState`/`useCallback` hooks, JSX via
`@vitejs/plugin-react`. See [`frameworks/keyed/README.md`](../README.md) for
how this fits alongside the other implementations.

```sh
npm install
npm run dev     # vite dev server
npm run build   # → dist/
```
