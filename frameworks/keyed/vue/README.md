# Vue (keyed) — js-framework-benchmark implementation

A single `<script setup>` SFC, Composition API (`ref`), via
`@vitejs/plugin-vue`. See [`frameworks/keyed/README.md`](../README.md) for
how this fits alongside the other implementations.

```sh
npm install
npm run dev     # vite dev server
npm run build   # → dist/
```
